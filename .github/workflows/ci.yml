name: CI

on:
  push:
    branches: [master]
  pull_request:
  schedule:
    # Weekly run catches new CVEs via govulncheck and toolchain drift.
    - cron: "0 6 * * 1"

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: .tool-versions
      - run: go build ./...
      - run: go test -race ./...

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: .tool-versions
      - uses: golangci/golangci-lint-action@v8
        with:
          version: v2.6.0

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: .tool-versions
      - name: gosec
        run: go run github.com/securego/gosec/v2/cmd/gosec@latest ./...
      - name: govulncheck
        run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...
