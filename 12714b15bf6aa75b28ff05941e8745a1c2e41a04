Session 2026-09-17: added /osm-secrets (ledger via $.ui.log, never model-facing), persisted detection provenance across the store, and cut 14 capture-group false positives found by replaying the live store. Format iterated table -> one bounded line per secret after real-session wrapping broke alignment; columns dropped on request. Verified: 87 unit, 21 hook, tsc, plugin validate, 25/25 scenario matrix.
